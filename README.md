# SolarCast Backend

Backend service for the SolarCast project.

## Setup

Install project dependencies:

```bash
uv sync
```

## Branches

* `main` → stable code
* `dev` → active development

Create feature branches from `dev`.

